# Dream Team

A multi-agent development team for Claude Code: six subagents, three commands, one manifest.

It knows nothing about your technology and nothing about your project. Everything specific is generated locally on first run and never committed. The same repository plugs into any codebase unchanged.

---

# Part 1 — Onboarding

## Install into a project

```bash
cd <your project>
git submodule add https://github.com/dm-23/dream-team.git .claude/team
```

Then make the agents and commands visible to Claude Code. Pick one of the two.

**Links** (commands keep short names: `/team`)

```bash
ln -s team/agents .claude/agents
for s in team generate-knowledge team-setup; do ln -s ../team/skills/$s .claude/skills/$s; done
```

On Windows without symlink permission, copy instead of linking:

```bash
cp -r .claude/team/agents .claude/agents
for s in team generate-knowledge team-setup; do mkdir -p .claude/skills/$s && cp .claude/team/skills/$s/SKILL.md .claude/skills/$s/; done
```

Copies must be refreshed after every update of the submodule, or the session keeps loading the old prompts while the team directory already holds the new ones. `/team-setup` compares the copies against the submodule and reports any that drifted, so run it after each update. Links do not have this problem.

**Plugin** (commands get a prefix: `/dream-team:team`)

Load `.claude/team` as a local plugin. For a permanent install add it to a local marketplace; for a quick trial start Claude Code with `--plugin-dir .claude/team`.

## First run

Open Claude Code in the project and run three commands in order.

| Step | Command | What it does |
|------|---------|--------------|
| 1 | `/team-setup fix` | Checks the install, hides generated files from version control, wires the sticky-mode hook, and asks about each optional capability separately before installing anything |
| 2 | `/generate-knowledge` | Explores your repository and writes the ten knowledge files the team reads |
| 3 | `/team <task>` | Runs the workflow |

Step 2 takes a few minutes and is the only step that reads your whole codebase. Repeat it after major architecture changes.

## Daily use

```
/team почини падение при пустом ответе сервиса
/team добавь страницу отчётов с фильтрами и экспортом
/team status      — where the current run stands
/team stop        — leave the run parked and return to normal chat
/team resume      — pick it back up
```

`/team` picks the workflow itself and announces it. You approve the plan before anyone writes code.

The two task lines above are not decoration. The team answers in whichever language you wrote the task in and records that language in the run's status file, so a run resumed days later still answers the way it started.

## What to expect

- You are asked up to a handful of clarifying questions, then shown a plan.
- Nothing is written until you approve.
- Every run leaves a diary in `.claude-tracking/`. Runs that change code also leave an entry in the learnings log; `Analyze` has no reviewer and so leaves none.
- An `Analyze` run answers in chat, or writes a Markdown file under the run's `reports/` when the findings are long.
- Ask for a report, in any wording, and you always get the same thing: a self-contained HTML file under the run's `reports/`, built from the report template, with its path given to you in chat. Nothing is published to the cloud. Say you want it some other way — a short answer in chat, Markdown, a particular path — and that message gets what you asked for; the next report request starts from the default again.
- The team reports the build and test results it actually ran, not a promise.

## Requirements

Claude Code, a version-controlled project, and a bash shell for the sticky-mode hook (Git Bash or WSL on Windows). Without bash everything else still works; only sticky mode is unavailable.

---

# Part 2 — How it works

## Three layers

| Layer | Where | Committed | Who owns it |
|-------|-------|-----------|-------------|
| Team core | `.claude/team/` (this repository) plus links or copies in `.claude/agents` and `.claude/skills` | yes, in this repository | team maintainer |
| Project knowledge | `.claude/knowledge/` | never | generated by `/generate-knowledge` |
| Run state | `.claude-tracking/` | never | written during runs |

The second and third layers are hidden from version control through `.git/info/exclude`, which is machine-local. `/team-setup fix` adds those lines. The committed ignore file is never touched, so a teammate who does not use the team sees nothing new.

This split is the whole design. Layer one carries process and roles. Layer two carries every fact about your stack, your commands and your rules. An agent never hardcodes a language, a framework, a build command or a lint rule; it reads them.

## Directory layout

```
.claude-plugin/plugin.json     identity when loaded as a plugin
team-manifest.json             the contract: what must exist, where, and what to check
agents/                        six role prompts
skills/                        three commands
templates/                     status, handoff, learnings, report, capabilities, and stack cards
hooks/                         sticky team mode
```

## The six roles

Each role is a separate subagent with its own tool set. The narrow tools are the enforcement, not a suggestion.

| Role | Tools | Does | Never does |
|------|-------|------|------------|
| Brainstorm | read only | Three parallel instances propose questions, solutions or a diagnosis | Reads your source; talks to you |
| ResearcherExplorer | read, search | Finds the exact files, symbols and insertion points | Writes anything |
| Architect | read, search, write to run state | Detailed plan and task breakdown for a full feature | Writes production code |
| Developer | read, edit, write, formatter and compile check | Implements exactly one task | Writes or runs tests |
| Tester | read, edit, write, runs only its own tests | Decides what needs tests and writes them | Touches production code |
| Reviewer | read, search, edit, write, full toolchain | Reviews against the generated checklist, runs format, lint, build and tests, records the learning | Adds features |

The orchestrator is the only role that talks to you. It never writes code and never runs a build.

**Brainstorm lenses.** The three instances are not clones. One argues for the smallest change, one hunts regressions and edge cases, one looks for what already exists in the codebase. A point agreed by two of three is accepted. A three-way split is not resolved silently: it comes back to you as a question.

**Model.** Brainstorm is the only role that pins one, in `agents/brainstorm.md`. Every other role inherits the session's. Since three instances run on every phase that uses them, that single line is the team's main cost lever — lower it there if a run costs more than it is worth to you.

## The five workflows

| Workflow | Trigger | Shape |
|----------|---------|-------|
| Analyze | explain, trace, find out | Research, then an answer. No code changes. |
| Bug Fix | something is broken | Research, three diagnoses, your approval, a surgical fix, review |
| Small Change | one concern, up to three files | Research, three proposals, your approval, implementation, review |
| Change Set | a list of independent small items | One research pass, batches with non-overlapping files run in parallel, one combined review |
| Full Feature | a new capability across modules | Questions, design, wide research, architecture, your approval of the plan, batched implementation, final review |

If research shows a Small Change is bigger than three files, or touches a schema, a public interface or wiring, you are asked whether to upgrade it rather than being quietly let through.

## Knowledge files

`/generate-knowledge` writes ten files into `.claude/knowledge/`. Six describe your codebase; four are the contract the agents run on.

| File | Read by | Contains |
|------|---------|----------|
| `PROJECT-OVERVIEW.md` | all | What the project is, its stack, its domains, its build facts |
| `BACKEND-ARCHITECTURE.md` | most | Layers, patterns, dependencies, anti-patterns, code paths that must change together |
| `FRONTEND-ARCHITECTURE.md` | most | The same for the user interface, or a note that there is none |
| `DI-AND-STARTUP.md` | architect, developer | Boot sequence, wiring, configuration, background work |
| `TESTING-CONVENTIONS.md` | tester, developer, reviewer | Frameworks, real examples, naming, what is deliberately not covered |
| `SEARCH-PLAYBOOK.md` | researcher | Verified search recipes for this repository |
| `TOOLCHAIN.md` | developer, tester, reviewer | Every command they may run, each marked verified or not, with its source |
| `CODING-STANDARDS.md` | developer, reviewer | Stack card plus your observed conventions; where they conflict, yours wins |
| `REVIEW-CHECKLIST.md` | reviewer | A flat numbered list the reviewer applies to every changed file |
| `PROJECT-RULES.md` | all | Obligations lifted from your own documentation, such as what must be updated alongside a change |

Two more files sit in the same directory and are written by someone else.

`LEARNINGS.md` persists across regenerations. The reviewer appends one entry per run plus an index row. When a run contradicts something a knowledge file claims, the reviewer marks it rather than editing that file; the next `/generate-knowledge` resolves the mark.

`CAPABILITIES.md` is written by `/team-setup` and describes the machine rather than the repository: which optional capabilities are installed here and the rules for each. It is rewritten on every setup run, so never edit it by hand. See the next section.

## Optional capabilities

The team ships with none and works fully without any. A capability is an accelerator: when one is present the work is better grounded, when it is absent nothing is blocked and no report ever blames its absence for a gap.

Four are declared in the manifest, each backed by the plugins that can provide it, and each installed only if you agree to that plugin by name. A capability may list more than one: `browser-control` ships two interchangeable entries, of which you install one.

| Capability | What it adds | Who it helps |
|------------|--------------|--------------|
| `library-docs` | Current, version-specific documentation for third-party libraries, so an external call is written against today's API instead of a remembered one | Research, architecture, implementation, review |
| `session-memory` | Recall of standing preferences and what earlier sessions concluded | The orchestrator, at preflight |
| `instructions-maintenance` | A reviewed way to update the project's own instruction file when a run changes a documented rule | The orchestrator, at docs sync |
| `browser-control` | Loading the running application to see what a change actually renders, and reading the real console and network behaviour | Testing and review |

Two rules hold for all of them. Whatever a capability returns is evidence to check, never truth: where it disagrees with your repository or the generated knowledge, the repository wins and the disagreement is reported. And a capability never moves work between roles — research stays with research, verification stays with review.

**Consent.** `/team-setup` reports which are present and which are missing, then asks about each missing one separately, saying what it is for. Nothing is installed without that answer. `/team` and the subagents never install, enable or disable anything.

**Where the wiring lives.** The concrete plugin names, marketplaces and install commands are in `team-manifest.json` under `plugins`, together with the `useWhen` and `neverUseFor` rules for each. `/team-setup` writes what it actually found into `.claude/knowledge/CAPABILITIES.md`, which the team reads. That file describes the machine rather than the repository, so it is rewritten on every setup run and never hand-edited.

**How agents reach them.** A subagent runs with the tool list in its own file, which does not include tools a plugin contributes. So by default the orchestrator uses a capability itself and passes the result into the handoff. If you would rather have a role call one directly, add that capability's tools to the role's file yourself; `/team-setup` reports which roles that would apply to and never edits them.

**Replacing an entry.** Point a manifest entry at a different plugin, keep its `capability` value, and nothing else changes: the rules and the roles follow the capability, not the vendor. Two entries may share a capability as alternatives, in which case install one, not both.

## Stack cards

A card is one file per language in `templates/standards/`, with a fixed set of twelve sections: how to detect the language, its toolchain, layout, naming, errors, concurrency, testing, dependencies, security, anti-patterns, review checks and sources. Run `ls templates/standards` to see which ship today.

`/generate-knowledge` matches every card's detection rules against your repository. Several can match at once, each labelled with the directories it applies to. It then annotates each rule against what your code actually does, marking it as followed, deliberately not followed, or irrelevant. Where a card and your codebase disagree, your codebase wins and the conflict is written down.

**Adding a language.** Copy `_generic.md`, keep the twelve section headings, fill them in. No agent changes.

## Sticky team mode

A run outlives the conversation that started it. While it is open, a marker file at `.claude-tracking/.team-mode` records the run, its workflow and its phase. A hook on every submitted prompt reads that marker and reminds the model that the run is active and which phase it is in.

This is deliberately not a memory or an instruction that lives in context. The harness runs the hook itself, so the behaviour survives a model switch, a long pause, a compaction and a full restart.

- The marker is written when a run starts, updated at every phase change, and deleted when the run closes.
- A message starting with a slash is never captured, so every command still works normally.
- `/team stop` deletes the marker and leaves the run parked; `/team resume` picks it up.
- If bash is missing, the hook exits quietly. Runs still work; they just do not follow you across sessions.

Wiring depends on the install. As a plugin, `hooks/hooks.json` applies by itself. With links, merge `hooks/settings-snippet.json` into the project's `.claude/settings.json`, which `/team-setup fix` offers to do. A newly added hook is picked up after opening `/hooks` once or restarting the session.

## The manifest

`team-manifest.json` is the single source of truth for what a correct install looks like. It lists the agents and commands, the required and persistent knowledge files, the template paths, the paths hidden from version control, the plugins the team expects, the hook wiring, and the neutrality check.

`/team` reads it on every invocation and refuses to start when a required knowledge file is missing. `/team-setup` validates every field. Adding a role, a command or a knowledge file means editing this file too.

**Plugins.** The `plugins` array declares the optional capabilities described above. Each entry names a capability, the plugin that provides it, its marketplace and install command, who uses it, and the rules for when it may and may not be used. `pluginPolicy` above the array states the three invariants: consent per plugin, equal quality without any of them, and brokering through the orchestrator. Add your own entries the same way.

## Neutrality check

The manifest carries a search pattern that must produce no output when run from the team root. It fails if any role prompt, command or this file names a language, a framework, a tool or a build command. Stack knowledge belongs in the cards and in generated knowledge, nowhere else. `/team-setup` runs this check and reports a hit as a defect in the team, not in your project.

Run it yourself before committing a change to any prompt.

## Updating the team

```bash
git submodule update --remote .claude/team
```

If you installed by copying rather than linking, refresh the copies in the same breath:

```bash
cp -r .claude/team/agents/. .claude/agents/
for s in team generate-knowledge team-setup; do cp .claude/team/skills/$s/SKILL.md .claude/skills/$s/; done
```

Then run `/team-setup check`. It compares every copy against the submodule and names the ones that drifted, which is the failure you get when a refresh is forgotten: the run silently uses the previous version of a role or a command. `/team-setup fix` re-copies them.

Existing knowledge files stay valid unless the manifest gained a required file, in which case the check tells you to regenerate.

## Troubleshooting

| Symptom | Cause | Fix |
|---------|-------|-----|
| An update to the team changed nothing, a role or command behaves like the previous version | Copies in `.claude/agents` or `.claude/skills` were not refreshed after the submodule update | `/team-setup fix`, then reopen the session |
| `/team` refuses to start | A required knowledge file is missing | `/generate-knowledge` |
| Sticky mode does nothing | Hook not wired, or no bash | `/team-setup check` reports which; open `/hooks` once after wiring |
| An unrelated request is pulled into a run | A stale marker from a run that never closed | `/team stop`, or `/team-setup fix` |
| A command is not offered | Agents or commands not linked or copied into `.claude/` | Repeat the install step |
| The reviewer reports a missing tool | A command in `TOOLCHAIN.md` is not installed here | Install it, or accept the gap; it is reported, never faked |
| Generated files appear in version control | The exclude lines were not added | `/team-setup fix` |
