# Everything here ships cross-platform and is checked out on machines with
# core.autocrlf=true. hooks/team-mode-prompt is a bash script and hooks/run-hook.cmd
# is a cmd/sh polyglot: a CRLF checkout breaks both, and with them sticky team mode.
# Pin LF in the working tree so a clone matches what was committed.
* text=auto eol=lf
